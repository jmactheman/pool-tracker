{
  "name": "pool-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only MCP server over the pool-tracker Supabase analysis views (service_role, server-side only).",
  "type": "module",
  "main": "server.js",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "dotenv": "^16",
    "zod": "^3"
  }
}
